{
  "name": "forkify",
  "version": "1.0.0",
  "description": "Recipe application",
  "scripts": {
    "start": "npm run cleanup && parcel index.html --no-cache",
    "build": "parcel build index.html --log-level verbose",
    "cleanup": "rimraf .parcel-cache dist",
    "dev": "npm run cleanup && parcel ./index.html"
  },
  "author": "Hadar Turgeman Gueta",
  "license": "ISC",
  "devDependencies": {
    "@parcel/transformer-sass": "^2.12.0",
    "parcel": "^2.12.0",
    "rimraf": "^3.0.2",
    "svgo": "^3.3.2"
  },
  "dependencies": {
    "core-js": "^3.37.1",
    "fractional": "^1.0.0",
    "regenerator-runtime": "^0.14.1",
    "sass": "^1.77.2"
  }
}
